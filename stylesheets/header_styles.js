import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    headerContainer: {
        display: 'flex',
        height: 60,
        width: '100%',
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginTop: 30,
        position: 'relative'
    }
});