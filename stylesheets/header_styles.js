import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    headerContainer: {
        display: 'flex',
        height: 60,
        width: '100%',
        flexDirection: 'row',
        marginTop: 30,
        position: 'relative'
    }
});